import { Id } from 'domain-api-base';
import { EventType, <%= eventDataTypeName %>, <%= eventTypeName %> } from './<%= eventFilename %>';

export const <%= functionName %> = (id: Id, data: <%= eventDataTypeName %>): <%= eventTypeName %> => ({
    id: id,
    type: EventType.<%= eventEnumFieldName %>,
    data: data,
});
