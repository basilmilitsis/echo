import { DomainEvent } from 'doma@echo/lib-domain-api';
import { <%= aggregateName %> } from '../<%= aggregateFileName %>';

export const <%= evolveFunctionName %> = (event: DomainEvent<string>): <%= aggregateName %> => {
    throw new Error('Not implemented');
}