import { DomainEvent } from '@echo/lib-domain-api';
import { <%=aggregateName%> } from '../<%=aggregateFileName%>';
import { <%=eventDataTypeName%> } from './<%=eventFileName%>';

export const <%=evolveFunctionName%> = (aggregate: <%=aggregateName%>, event: DomainEvent<string, <%=eventDataTypeName%>>): <%=aggregateName%> => {
    throw new Error('Not implemented');
}