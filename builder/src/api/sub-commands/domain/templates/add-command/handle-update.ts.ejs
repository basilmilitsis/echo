import { CommandEvent, HandleUpdateCommand } from '@echo/lib-domain-api';
import { <%= commandTypeName %> } from './<%= commandFileName %>';
import { <%= aggregateTypeName %> } from '../<%= aggregateFileName %>';

export const <%= functionName %>: HandleUpdateCommand<<%= commandTypeName %>, <%= aggregateTypeName %>> = (
    command: <%= commandTypeName %>,
    aggregate: <%= aggregateTypeName %>
): CommandEvent[] => {
    return [];
};
