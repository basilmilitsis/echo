import { CommandEvent, UpdateHandler } from '@echo/lib-domain-api';
import { <%= commandTypeName %> } from './<%= commandFileName %>';
import { <%= aggregateTypeName %> } from '../<%= aggregateFileName %>';

export const <%= functionName %>: UpdateHandler<<%= commandTypeName %>, <%= aggregateTypeName %>> = (
    command: <%= commandTypeName %>,
    aggregate: <%= aggregateTypeName %> | undefined
): CommandEvent[] => {
    return [];
};
