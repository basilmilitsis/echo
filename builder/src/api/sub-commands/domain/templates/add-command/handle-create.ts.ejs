import { CommandEvent, CreateHandler } from 'domain-api-base';
import { <%= commandTypeName %> } from './<%= commandFileName %>';

export const <%= functionName %>: CreateHandler<<%= commandTypeName %>> = (
    command: <%= commandTypeName %>
): CommandEvent[] => {
    return [];
};
