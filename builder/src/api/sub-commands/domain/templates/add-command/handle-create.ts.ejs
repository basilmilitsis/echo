import { CommandEvent, HandleCreateCommand } from '@echo/lib-domain-api';
import { <%= commandTypeName %> } from './<%= commandFileName %>';

export const <%= functionName %>: HandleCreateCommand<<%= commandTypeName %>> = (
    command: <%= commandTypeName %>
): CommandEvent[] => {
    return [];
};
